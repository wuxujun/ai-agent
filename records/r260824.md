# Wiki 当前能力与本地搜索优化分析

日期：2026-08-24  
项目：`github.com/wuxujun/ai-agent`  
结论：Wiki 只读链路已可用且当前实例健康；本地搜索适合小型、标题规范的知识库，下一阶段应先扩大 Eval 与建立可解释基线，再实施倒排索引、BM25、CJK n-gram 和不可变增量快照。向量检索与 Wiki 回写继续后置。

## 一、当前运行状态

2026-08-23 对本地 8088 实例执行 `GET /ready`，结果为：

- 服务整体 `ready=true`。
- `wiki.configured=true`、`wiki.healthy=true`、`wiki.required=false`。
- Team 配置健康，7 个 Team、0 个无效引用。
- 当前运行实例的 active Team 为 `wiki_suggest`。
- `teams.yaml` 文件中的 `active_team` 为 `kb_qa`，表明当前实例由环境变量或启动配置覆盖；这是支持的行为，排障时应区分文件值与运行值。

定向测试命令：

```bash
go test ./internal/wiki ./internal/tools ./cmd/server ./internal/planner \
  ./internal/multiagent ./internal/api ./cmd/wiki-eval
```

上述测试已全部通过。沙箱内首次执行时部分 `httptest` 因禁止监听随机端口失败，在允许本地监听的宿主环境重跑后通过，未发现 Wiki 断言失败。

## 二、当前 Wiki 支持范围

### 2.1 数据源

- 本地目录模式：`AI_AGENT_WIKI_DIRECTORY` 可指向包含 `AGENTS.md/raw/wiki` 的仓库根，也可直接指向 `wiki/`。
- 远程模式：通过 Streamable HTTP MCP 连接 LLM Wiki。
- 本地目录与远程 MCP 模式互斥。
- Wiki Runtime 配置属于重启生效项，尚不支持在不重建 Runtime 的情况下完整热切换。
- `required=true` 时初始化失败阻止启动；可选 Wiki 初始化失败不阻止主服务，但不会注册可用工具。

### 2.2 工具链

| 工具 | 状态 | 用途与限制 |
| --- | --- | --- |
| `wiki_search` | 已支持 | 搜索候选，`top_k` 上限为 10 |
| `wiki_fetch` | 已支持 | 只能读取同一任务搜索产生的候选 ID，受数量和字节预算限制 |
| `wiki_graph` | 条件支持 | 后端支持时注册，深度限制为 1–2，每任务最多一次 |
| `wiki_graph_fetch` | 条件支持 | 读取有界图邻居页面，最多 3 个 URI |
| `wiki_suggest` | 条件支持 | 后端支持时注册，每任务最多一次，只生成策展候选 |

本地目录客户端同时支持 Search、Read、Graph 和 Suggest，因此本地模式会注册上述完整只读工具链。远程模式根据 MCP 实际 Schema 发现 Graph/Suggest，不注册占位工具。

### 2.3 Multi-Agent Team

- `wiki`：固定 `wiki_search -> wiki_fetch`，用于普通 Wiki 问答。
- `wiki_graph`：受控执行搜索、全文读取、图遍历和邻居读取。
- `wiki_suggest`：受控执行搜索、全文读取和只读策展建议。
- `kb_qa` Writer 也包含 Wiki 证据与引用规则，但是否选择 Wiki 仍取决于 JIT 路由、Team allowlist 和实际注册工具。
- 当前 Wiki Team 已配置 Langfuse Prompt 名称与 `production` 标签；相关工作区修改尚未提交。

### 2.4 安全、隔离与可靠性

- 每次执行按 Tenant 解析 `api.tenants.<tenant>.wiki_space`，否则使用显式 `wiki.default_space`。
- 非默认租户缺少 Space 且无默认 Space 时明确失败，避免静默跨租户检索。
- 本地读取仅接受普通 Markdown 文件，限制单页大小，保留路径穿越与符号链接边界检查。
- Wiki 内容被视为不可信证据，继续经过 Prompt Guard、Evidence Filter 和 Writer 约束。
- 搜索候选 ID 绑定 Tenant 和 Task，不能跨任务直接用于 `wiki_fetch`。
- 搜索与读取共享超时、熔断、半开探针、readiness 和 OTel/本地指标。
- `wiki_suggest` Writer 只接收建议证据，不能根据搜索结果或全文自行扩展建议。

### 2.5 明确未支持的能力

- `wiki://` 是稳定证据 URI，不是浏览器可直接打开的 URL；当前没有受控页面解析 HTTP API。
- 没有实际 Wiki create/update/delete/ingest/commit 工具或 API。
- `internal/wiki/writeback.go` 仅实现无副作用的提案校验、hash、幂等键和 diff 预览，未接入工具注册、审批或实际写入。
- 没有页面历史、版本/commit 引用、策展审核队列和多 Space 请求选择能力。
- `config.yaml` 注释仍称 suggestion 不注册，但实现会按后端能力注册 `wiki_suggest`，存在文档漂移。

## 三、本地搜索当前实现

当前本地检索流程：

```text
Search
  -> WalkDir 枚举全部 Markdown，并生成 path + size + mtime 签名
  -> 签名变化时重建全部页面索引
  -> 规范化 query，并生成去空格 compact phrase
  -> 遍历全部页面执行 Contains/Count 评分
  -> 对直接链接页面做一次受限加权
  -> 全量排序并返回 TopK
```

索引字段包括：

- slug、标题、所有 Markdown heading。
- frontmatter 的 title、summary、tags、aliases。
- 正文规范化文本与 compact 文本。
- 最多 256 个非空行式 chunk，每个最多 500 rune，用于生成较相关的 excerpt。
- 标准 Markdown `.md` 链接，用于一跳关系召回、Graph 和 Suggest。

当前权重偏向标题和完整短语：title phrase 100、slug phrase 70、heading 40、metadata 35、body 20；term 频次按字段继续加分，正文单 term 最多计 10 次。导航页 `index`、`_index`、`log` 会降权。

## 四、本地搜索优势与瓶颈

### 4.1 已有优势

- 完全本地、确定性、无模型费用，远程依赖失败不影响目录搜索。
- 对精确标题、slug、frontmatter alias 和中英文标点/空格变化效果较好。
- 同分按 slug 排序，结果可复现。
- 内容变更能够触发刷新，并已有并发搜索回归测试。
- 搜索结果天然保留 `wiki://` URI，便于证据引用和租户隔离。
- 已有 Recall@K、First Hit、MRR、Fetch、Keyword、Citation、Graph、Suggest 和 P95 Eval 指标。

### 4.2 主要瓶颈

1. **每次搜索都扫描目录**：即使索引未变化，也要 `WalkDir` 计算签名；Wiki 扩大后文件系统开销会进入查询延迟。
2. **不是倒排索引**：查询遍历全部页面，并对多个完整字符串执行 `Contains/Count`，复杂度随页面数和总文本线性增长。
3. **中文分词不足**：连续中文通常成为一个 term；compact 只能处理空格和标点，不能稳定处理部分词、词序变化和同义表达。
4. **人工权重缺乏统计归一化**：没有 IDF 和文档长度归一化，常见词、重复 metadata 或长正文可能影响排序。
5. **链接加权可能带来噪声**：强匹配页面可提名所有直接链接页面，即使邻居本身不匹配 query。
6. **刷新粒度过粗**：任意文件变化都会重建全部索引；签名只依赖路径、大小和 mtime，缺少内容摘要与文件级增量状态。
7. **链接格式覆盖不完整**：主要识别标准 Markdown `.md` 链接，纯 `[[wikilink]]`、无扩展名链接等形式可能缺失。
8. **Eval 样本不足**：当前 `evals/wiki_retrieval.jsonl` 只有 5 条，集中在 PBL 主题，容易对现有人工权重过拟合。

## 五、推荐优化路线

### P0：质量基线与可解释性

先保持默认排序行为不变，建立可以判断优化是否真实有效的基线：

1. 将 Eval 扩充到至少 30–50 条，覆盖精确标题、部分标题、别名、中英文混合、词序变化、多正确答案、相似标题、高频通用词、导航页干扰和无答案查询。
2. 增加 Precision@K、NDCG@K、Top1/Top3 命中率、零结果率和无答案误召回率。
3. 增加冷/热查询 P50/P95、索引刷新耗时、页面数和索引内存估算。
4. 为搜索增加内部 explain 结构，报告命中字段、phrase、基础分、链接加权和索引版本；默认 API 不必暴露。
5. 增加 100、1,000、10,000 页面合成 Benchmark，固化当前算法基线。

### P1：倒排索引、BM25 与 CJK n-gram

1. 引入不可变 `directoryIndexSnapshot`，保存页面、posting、document frequency、字段长度、平均长度、版本和构建时间。
2. 英文按 Unicode 单词切分；数字保留年代、范围和常见课程编号。
3. CJK 段落生成受限二元组，必要时三元组，同时保留完整标题/alias phrase。
4. 使用字段化 BM25，建议初始权重：title 8、slug 6、aliases 5、headings 4、summary/tags 3、body 1。
5. 在 BM25 上叠加完整标题、alias 和连续 phrase 加分，继续保留确定性 slug tie-break。
6. 旧评分器保留为 feature flag/fallback，通过同一 Eval 对比后再切换默认实现。

### P1：增量不可变快照

1. 周期性扫描或显式 Refresh 替代每次查询扫描；`fsnotify` 只作为刷新提示，不能作为唯一一致性机制。
2. 保存文件级 path、size、mtime 和可选内容 hash，只重建新增或变化页面，并移除已删除页面的 posting。
3. 在锁外构建新快照，完整验证后原子替换；构建失败继续提供上一份健康快照。
4. readiness/metrics 增加索引版本、页数、最后成功时间、刷新耗时、失败数和当前是否 stale。

### P1：链接召回校准

- 先生成纯词法 TopN，再只从高置信种子扩展一跳。
- 邻居必须自身命中 query token，或属于明确的 entity/source/comparison 关系。
- 使用绝对上限控制 link boost，不让链接关系覆盖强标题匹配。
- 导航页不能作为扩展种子。
- explain 中记录 `link_boost_source`，方便定位噪声来源。

### P2：可选混合检索

BM25 基线稳定后再增加向量召回：

```text
BM25 Top50 + Vector Top50 -> Reciprocal Rank Fusion -> 可选 Reranker -> TopK
```

要求：Embedding 不可用时自动退回 BM25；向量索引绑定 Wiki snapshot/version；不同 Tenant Space 不共享候选；默认继续支持完全离线运行。LLM 查询改写和 LLM reranker 不应进入第一阶段。

## 六、建议下一步实施内容

下一阶段建议只实施“本地搜索 P0”，不要同时引入向量检索：

1. 扩充检索 Eval 数据集。
2. 增加 Precision@K、NDCG@K 和无答案误召回指标。
3. 增加当前评分器的结构化 explain。
4. 增加大目录 Benchmark 和索引刷新测试。
5. 保存当前质量与性能基线。
6. 基线稳定后单独实施 BM25/CJK n-gram，并以 feature flag 灰度。

建议初始验收门槛：

```text
Recall@5                 >= 0.90
Top1 hit rate            >= 0.75
NDCG@5                   >= 0.85
无答案误召回率            <= 0.10
wiki:// citation coverage = 1.00
错误率                    = 0
1,000 页热查询 P95        <= 100ms
10,000 页热查询 P95       <= 300ms
索引更新失败时旧快照仍可查询
```

推荐顺序：

```text
扩大 Eval -> 固化基线 -> 可解释评分 -> 倒排索引/BM25
          -> CJK n-gram -> 增量快照 -> 链接校准 -> 可选混合检索
```

## 七、暂缓事项

- Wiki 实际回写继续暂停；现有 proposal preview 不等同于写入支持。
- `wiki://` HTTP 页面解析入口可独立排期，但不应与搜索排序重构耦合。
- 多 Space、自助选择 Space、历史版本和策展审核队列在检索基线稳定后推进。
- 向量数据库、Embedding 批处理和 LLM reranker 暂不进入下一阶段。

## 八、P0 第一批实施进展

已完成：

- Wiki Eval 增加 Precision@K、NDCG@K 和 Top3 命中率。
- Eval Case 支持 `expect_no_results=true`，并增加无答案误召回率与质量门槛。
- `cmd/wiki-eval` 增加对应 CLI 参数、文本输出和 JSON 字段。
- 新增无答案数据集校验、误召回门禁和 CLI 回归测试。
- 新增 100、1,000、10,000 页的本地搜索 Benchmark；普通 `go test` 只编译，不执行基准。

2026-08-24 单次热查询基线：

```text
100 页：      4.74ms，约 1.05MB/op，8,968 allocs/op
1,000 页：   45.23ms，约 10.51MB/op，89,109 allocs/op
10,000 页： 462.78ms，约 107.37MB/op，890,240 allocs/op
```

结果确认当前查询时间与内存分配随页面数量近似线性增长，10,000 页已超过建议的 300ms 热查询门槛。下一批应优先实现评分 explain 与可信 Eval 扩充；真实数据集标签不足时不应为了达到条数而伪造案例。随后进入倒排索引/BM25 与查询路径移除 `WalkDir`。

P0 第二批已完成结构化评分 explain：本地 `DirectoryClient` 新增仅供 Eval/诊断使用的 `SearchWithExplain`，报告 URI、索引版本、基础分、字段得分、phrase 命中、命中词、链接加权、导航页倍率和最终分。`cmd/wiki-eval` 的 JSON case 结果会包含 explain；生产 `wiki_search` 继续调用原 `Search`，API 契约和默认计算路径不变。回归测试锁定两条路径的 URI、顺序和最终得分完全一致。

P0 第三批已基于实际目录 `/Users/xujunwu/docker_data/comp-wiki` 的真实 frontmatter 标题和 slug，将 `evals/wiki_retrieval.jsonl` 从 5 条扩充到 30 条：25 个正例覆盖 comparisons、concepts、entities 和外部/目录 sources，5 个负例使用确定性 nonce 并声明 `expect_no_results=true`。未修改该外部 Wiki 的任何页面。

真实 30-case 基线：

```text
Recall@5                    0.960
Precision@5                 0.192
NDCG@5                      0.857
Top1 hit rate               0.680
Top3 hit rate               0.960
MRR                         0.820
Fetch/keyword coverage      0.960 / 0.960
wiki:// citation coverage   1.000
No-answer false positive    0.400
P95                         39ms
Graph recall/noise          1.000 / 0.970
Suggestion recall/noise     1.000 / 0.600
```

质量门禁按预期未通过：Top1 低于 0.75、无答案误召回高于 0、Graph 无关节点率高于 0.75。具体缺口包括“PBL 历史旅行指南课程来源”未进入 Top5，多个核心页只排第 2，以及包含通用英文词的 nonce 查询产生部分词误召回。该失败基线应保留，作为倒排索引/BM25、最小词项匹配和链接扩展校准的对照，不能通过放宽标签或删除失败案例规避。

## 九、P1 BM25 第一批实施进展

新增可回退的本地搜索模式 `wiki.local_search_mode`，可选 `legacy` 或 `bm25`，环境变量为 `AI_AGENT_WIKI_LOCAL_SEARCH_MODE`，默认仍为 `legacy` 且修改后需要重启。BM25 模式在目录索引刷新时构建字段化 posting，使用 title、slug、heading、metadata、body 权重 8/6/4/3/1，包含 IDF、长度归一化、英文/数字 token、CJK 二元组、phrase boost、60% 最小词项覆盖、受限链接加权和导航页降权。Legacy 与 BM25 使用同一只读边界，可随配置回退。

同一真实 30-case BM25 结果：

```text
Recall@5 / NDCG@5           1.000 / 1.000
Top1 / Top3 / MRR           1.000 / 1.000 / 1.000
No-answer false positive    0.000
Fetch/keyword/citation      1.000 / 1.000 / 1.000
P95                         20ms
```

BM25 修复了 Legacy 的课程来源漏召回、核心页第 2 名和 nonce 部分词误召回。完整门禁仍因 Graph 无关节点率 0.970 失败；该项与文本排序独立，应在后续 Graph 校准阶段处理。

单次热查询对照 Benchmark：

```text
页面数    Legacy       BM25
100       6.11ms       4.54ms
1,000     46.04ms      34.87ms
10,000    470.57ms     323.23ms
```

BM25 已改善排序与查询 CPU，但 10,000 页仍超过 300ms，单次分配仍约 114MB。根因是每次查询仍执行目录 inventory 扫描并复制页面快照；下一批应实现定时/显式刷新与原子不可变快照，使热查询不再调用 `WalkDir`。

## 十、P1 不可变索引快照

本地目录索引现以原子指针发布完整只读快照，快照同时包含页面、版本与 BM25 posting。`Search`、`Graph` 和 `Suggest` 直接复用同一代快照，不再逐请求复制页面或执行 `WalkDir`。新增 `wiki.local_refresh_interval_seconds`（环境变量 `AI_AGENT_WIKI_LOCAL_REFRESH_INTERVAL_SECONDS`），默认 30 秒；普通读取在间隔到期时由单个请求惰性检查，配置为 0 时仅由 `Probe` 显式检查。无需后台 goroutine，关闭流程不增加生命周期负担。

刷新先完整构建候选索引，再一次性发布。普通查询遇到重建错误时继续使用最后一份完整快照；`Probe` 会返回实际错误用于健康检查。初始化仍失败关闭，查询上下文取消也不会被旧快照掩盖。回归测试覆盖自动刷新、Probe-only、失败保留旧快照、负配置和 race 并发访问。

相同宽查询的快照化 Benchmark：

```text
页面数    Legacy       BM25
100       1.72ms       0.75ms
1,000     25.23ms      11.17ms
10,000    266.77ms     120.85ms
```

与上一批相比，10,000 页 BM25 从 323.23ms 降到 120.85ms，约降低 62.6%；分配从约 114MB/op 降到约 10MB/op。剩余成本主要来自宽查询命中大量 posting 后的候选评分、结果 map 和 excerpt 计算，而不是目录扫描。下一步应为 BM25 加入候选上界/稀有词优先与 Top-K 堆，再处理 Graph 无关节点率门禁。

## 十一、P1 BM25 Top-K 热路径

BM25 查询评分由按文档分配的 map/pointer 改为连续评分数组，仅在 explain 模式创建字段明细；slug 到文档编号的映射随不可变索引快照预构建。词项覆盖筛选后使用固定容量 Top-K 最小堆，仅为最终结果生成 `Document` 和相关 excerpt，避免宽查询先构造全部结果再全量排序。评分公式、phrase boost、链接加权上限、导航页降权及 slug 确定性 tie-break 均保持不变。

真实 30-case Eval 保持：Recall@5、NDCG@5、Top1、Top3、MRR、Fetch、Keyword、Citation 均为 1.000，无答案误召回为 0，P95 从 20ms 降至 1ms。Graph 无关节点率仍为 0.970，是当前唯一独立门禁缺口。

相同宽查询的新 Benchmark：

```text
页面数    Legacy       BM25
100       1.64ms       0.054ms
1,000     25.15ms      0.273ms
10,000    276.09ms     2.43ms
```

10,000 页 BM25 相比不可变快照首版的 120.85ms 再降低约 98%，分配降至约 1.32MB/op、52 allocs/op。当前无需引入可能改变召回的候选硬截断；下一阶段可直接校准 Graph 遍历与无关节点控制。

## 十二、P1 Graph 扩散校准

本地 `Graph` 原先按文件链接顺序执行两层 BFS，高出度的项目设计页、来源目录页会迅速触发 100 节点全局上限。现改为逐层收集候选，并按根页面标题在候选页面 title、slug、heading、metadata、body 中的相关性排序；导航页降权，同分按 slug 稳定排序。只有选中节点及其真实连接边进入结果，避免出现指向已裁剪节点的悬空边。

新增 `wiki.local_graph_max_nodes`（环境变量 `AI_AGENT_WIKI_LOCAL_GRAPH_MAX_NODES`），默认 12、允许 1–100，配置 0 使用内置默认值。真实 PBL 二跳图从 100 节点、无关率 0.970 降至 12 节点、无关率 0.727，三个期望节点全部保留，Graph recall 维持 1.000。至此当前 30-case 的搜索、无答案、引用、Graph 和 Suggestion 门禁全部通过。

## 十三、P1 默认启用 BM25

在真实 30-case 全部门禁通过后，运行时配置与 `cmd/wiki-eval` 的默认本地排序器从 `legacy` 切换为 `bm25`。`AI_AGENT_WIKI_LOCAL_SEARCH_MODE=legacy` 继续作为重启后立即回退路径；底层 `NewDirectory` 未传 option 时仍保留 Legacy 兼容行为，避免无配置的库调用方发生隐式语义变化。服务启动日志增加 `search_mode`、刷新间隔和 Graph 节点上限，便于确认实际生效配置，不记录页面内容或鉴权信息。

## 十四、P1 索引运行状态

本地目录 client 新增内容无关的状态快照：backend、搜索模式、页数、索引版本、构建时间、最近刷新尝试/成功时间、刷新耗时、连续失败数、是否正在服务旧快照、刷新间隔和 Graph 上限。状态不包含目录路径、页面标题、摘要、查询词或鉴权信息。`/ready` 的 `wiki.status` 返回该信息；远程 MCP 模式仅返回 `backend=remote`。索引重建失败时 readiness 的 Probe 仍失败，同时状态明确标记 `serving_stale_snapshot=true`，便于区分“完全不可用”和“健康检查失败但旧快照仍可查询”。

## 十五、线上验证与近似课程隔离

8088 重启后 `/ready` 返回 200，确认 `search_mode=bm25`、419 页、Graph 上限 12、刷新失败数 0 且未服务旧快照。真实 `team=wiki` 任务成功调用 `wiki_search` 与 `wiki_fetch`，前两名为目标概念页和课程来源页，并生成 `wiki://` 引用。

验证同时发现第三名通用 `sources/raw-doc-*` 页面描述另一版本的相似课程，writer 将其中 Estelle 与目标课程 Vanessa 合并。为降低此类实体串线，BM25 对“查询未精确匹配标题”的通用 raw-doc source note 使用 0.2 来源特异性倍率；标题精确匹配 raw-doc 时倍率仍为 1，保留直接查询能力。Explain 新增 `source_multiplier`。Wiki writer 规则同步要求优先精确标题页面及其明确链接来源/实体，不得合并名称相近 offering 的人物、日期、门槛或交付细节，无法消解时报告冲突。

第二次线上任务确认串线已消失：Top-5 为目标概念、课程来源、课程设计、CT/NCT 对比和 Vanessa 实体，最终答案仅报告 Vanessa。但自动 fetch 原先仍机械读取前三名，导致实体页虽在 Top-5 却未直接读取。后续选择改为保留第一名后，在有限预算内优先覆盖 sources、entities、comparisons、concepts 等不同页面类别，再按原排名补齐。单元测试已确认候选集同时含有来源页和实体页时，三个 fetch 会选取目标概念、课程来源和 Vanessa 实体，减少重复概念页占用证据预算。

第三次重启后线上任务 `wiki-bm25-live-260824-v4` 确认多样化选择已生效：自动 fetch 读取目标概念页、课程来源页和 CT/NCT 对比页，不再读取通用课程设计页。本次查询的 Top-5 未包含 Vanessa 实体页，因此有限候选集无法直接选取该页；导师姓名、学历和批改经验仍由课程来源页直接支撑。最终答案仅报告 Vanessa Ruales，正确说明 800–1,000 字产出与 6 CT + 1 NCT 结构，并引用三个已读取页面；近似 raw-doc 页虽位于第五名，未被 fetch 且未污染答案。

## 十六、P1 候选发现与全文读取预算解耦

为解决实体页偶尔排在默认 Top-5 之外、导致类别多样化无候选可选的问题，`wiki.search_top_k` 默认值由 5 调整为 8，`wiki.fetch_max_items` 继续保持 3。这使搜索阶段能看到更多轻量标题与摘要，再从中按 sources、entities、comparisons、concepts 类别选取最多三页全文；不增加 Wiki 全文读取次数、证据字节上限或 LLM 调用次数。

主配置、中文注释配置和内建默认值已同步。针对性测试、全仓 `go test ./...` 及 config/tools/multiagent/server 竞态测试均通过。该配置修改需重启服务后才会应用到线上任务。

重启后线上任务 `wiki-bm25-live-260824-v5` 完成闭环验收：`wiki_search` 返回 8 个候选，Vanessa Ruales 实体页位于第 6 名；多样化选择在三页 fetch 预算内读取目标概念页、课程来源页和 Vanessa 实体页。第 5 名的近似 raw-doc 候选没有被读取，最终答案仅引用上述三页，正确报告 800–1,000 字产出、6 CT + 1 NCT 结构及 Vanessa Ruales，未出现相似课程导师串线。

## 十七、配置与中文镜像同步

修正 Wiki 配置注释与实际工具注册行为的偏差：`wiki_search` 与内部 `wiki_fetch` 始终注册，后端支持时还会注册只读 `wiki_graph` 和 `wiki_suggest`；写入、摄取、提交、Schema 与 Space 管理仍不开放。主配置和中文注释已同步，README 原有行为说明已与代码一致，无需改动。

`teams_zh.yml` 的 `active_team` 已与主 `teams.yaml` 同步为 `kb_qa`，三个 Wiki Team 的 planner/writer 也补齐主配置中的 Langfuse `prompt_name` 与 `prompt_label: production`。中文文件仍只是注释镜像，不代替运行时主配置。四个 YAML 解析验证及 config/multiagent/server 测试均通过。

## 十八、P1 Wiki 引用完整性审计

答案流水线升级为 `p2-v2`，新增无 LLM 调用的确定性 `wiki_citation_integrity` 阶段。该阶段只把当前任务 `wiki_fetch` 或 `wiki_graph_fetch` Trace 中的 `wiki://` Evidence Path 视为已读取证据，不会将 `wiki_search` 结果候选误当作已读取页面。它检查答案是否缺少 Wiki 引用、URI 格式是否合法，以及每个引用是否精确存在于已读取集合中。未读取页、仅搜索候选、跨 Space URI 和编码路径穿越会生成有界的 typed finding；有效引用则标记 `passed`。

策展 Team 需要引用尚未全文读取的建议目标，因此 `wiki_suggest` 显式记为 `not_applicable`，避免误报。新阶段已加入配置必需阶段合法性校验和审计 API stage 过滤白名单。当前全局 `enforcement=observe`，所以 finding 只记录、不改写或阻断答案。回归用例覆盖有效引用、伪造/未读取 URI、跨 Space、非法路径、缺少引用和策展豁免；全仓测试与 answerpipeline/api/orchestrator/config 竞态测试均通过。

重启后线上任务 `wiki-citation-live-260824-v6` 确认流水线已升级为 `p2-v2`，新阶段不消耗 Token，并且准确发现 Writer 的 References 将完整 `wiki://local/...` Evidence Path 缩写成了 `concepts/...`、`sources/...` 和 `entities/...` 相对 slug，因此返回 `warned / missing_wiki_citation`。该结果是真实格式缺陷，不是误报。Wiki、Wiki Graph 和 Wiki Suggestion 的中英文 Writer 规则已同步加强：必须逐字使用完整 `wiki://` URI，禁止改写为相对 slug。YAML 解析、multiagent 和 server 测试已通过；该 Prompt 修正需再次重启后完成 `passed` 线上验收。

再次重启后，线上任务 `wiki-citation-live-260824-v7` 完成最终闭环：Team digest 更新为 `b460e18454ffbfae90fcbff5`，证明新 Writer Prompt 已加载；References 逐字输出三个已读取的完整 `wiki://local/...` Evidence Path。`wiki_citation_integrity` 返回 `passed`，无 findings，Token 使用为 0，答案保持 `publishable=true`。搜索仍返回 8 个候选，全文只读取目标概念、课程来源和 Vanessa 实体三页，近似 raw-doc 未进入证据集。至此“实际 fetch 证据 → Writer 完整 URI → 无 LLM 引用审计”链路已通过真实 8088 任务验收。

## 十九、P1 Wiki 引用审计执行策略

答案流水线升级为 `p2-v3`，`wiki_citation_integrity` 加入默认 `required_stages`，但生产主配置继续使用 `enforcement=observe`。同一引用 finding 在 observe 下仍返回 `warned`，任务状态与发布不受影响；在 advisory 下返回 `failed`，必需阶段策略将任务降级为 `partial` 但保留答案；在 strict 下同样降级任务，并设置 `publishable=false`。完整且实际已读取的 Wiki 引用在三种模式下都保持 `passed` 和可发布。

阶段 fingerprint 现包含 enforcement 模式，避免从 observe 重审计到 strict 时错误复用旧 `warned` 结果。租户 `answer_pipeline_enforcement` 与 `answer_pipeline_required_stages` 覆盖已通过回归测试。测试覆盖 observe/advisory/strict 三种模式、有效 strict 引用、跨模式重审计和租户策略；全仓测试及 answerpipeline/config/api/orchestrator 竞态测试全部通过。该代码与默认必需阶段变更需重启服务，重启后仍应先保持 observe 观察，再按租户灰度 advisory。

重启后线上任务 `wiki-citation-policy-live-260825-v8` 确认 `p2-v3` 已生效：全局 enforcement 仍为 `observe`，`wiki_citation_integrity` 返回 `passed`、无 findings、Token 为 0，任务保持 `completed` 且 `publishable=true`。Writer 引用的三个完整 `wiki://local/...` URI 与本次 `wiki_fetch` Evidence Path 完全一致，近似 raw-doc 仍未被读取。`GET /api/audits?stage=wiki_citation_integrity` 同时成功返回历史 warned/passed 审计，证明新阶段的运维过滤入口可用。当前不需要再次重启。

## 二十、P1 Wiki 候选缓存生命周期治理

Wiki 搜索候选仍只保存在进程内且按“租户 + 任务”隔离，但不再仅依赖后续请求触发的 30 分钟惰性清理。`Engine.Next` 在任务进入 completed、partial 或 failed 终态时主动调用租户级精确释放；重复释放幂等，不会误删相同任务 ID 的其他租户缓存。读取未知候选也不再创建空任务项，避免无效 fetch 消耗容量。

新增 `wiki.candidate_cache_max_tasks`（环境变量 `AI_AGENT_WIKI_CANDIDATE_CACHE_MAX_TASKS`，默认 1024）与 `wiki.candidate_cache_ttl_seconds`（环境变量 `AI_AGENT_WIKI_CANDIDATE_CACHE_TTL_SECONDS`，默认 1800）。被中断或遗留任务由 TTL 清理；达到容量时按最后访问时间执行确定性 LRU 淘汰，同时间戳按任务键稳定决胜。配置为 0 时使用内置默认值，负值拒绝启动。配置随 Wiki 运行时初始化，修改后需重启。

`GET /api/metrics` 的 Wiki 快照新增当前候选缓存任务数，以及终态释放、TTL 过期和容量淘汰累计数；OpenTelemetry 同步输出 `agent.wiki.candidate_cache.tasks` UpDownCounter 和带有限 `reason=terminal|expired|capacity` 标签的 removal Counter，不包含租户、任务、查询或页面等高基数/敏感标签。

回归测试覆盖未知候选不建项、TTL 清理、确定性容量淘汰、租户隔离、幂等终态释放、指标变化和并发生命周期。定向测试、全仓 `go test ./...` 以及 `go test -race ./internal/tools ./internal/orchestrator` 全部通过。该配置与终态释放代码需服务重启后生效；尚未进行 8088 线上指标闭环验证。

服务重启后，线上任务 `wiki-cache-lifecycle-live-260825-v9` 完成生命周期闭环。`/ready` 返回 Wiki 本地 BM25 索引健康、419 页、未服务旧快照；初始缓存任务数为 0。任务显式选择 `team=wiki`（避免当前全局默认 `wiki_suggest` 干扰），运行中完成 4 次 Wiki 后端调用，缓存任务数升至 1；进入 completed 终态后立即回落至 0，`candidate_cache_released` 从 0 增至 1，过期与容量淘汰保持 0。

本次搜索返回 8 个候选，自动全文读取目标概念页、课程来源页和 Vanessa Ruales 实体页，未读取近似 raw-doc 页面。最终答案正确报告 800–1,000 字核心产出、6 CT + 1 NCT 授课结构及 Vanessa Ruales，三个 References 均为实际读取的完整 `wiki://local/...` URI；`wiki_citation_integrity` 为 `passed`、无额外 Token，任务保持 `publishable=true`。候选缓存终态释放功能现已完成代码、竞态测试和真实服务验证。

## 二十一、P2 Wiki 只读页面访问 API

新增受统一认证保护的 `GET /api/wiki/pages/:space/*slug`，用于将答案中的稳定 `wiki://` 证据标识解析为可由普通 HTTP 客户端读取的页面。接口复用服务已初始化的同一个只读 Wiki client，本地目录和远程 MCP 都继续经过各自 reader 的路径、Markdown、大小、schema、超时与网络策略，不直接接受文件系统路径，也没有新增 write、apply、ingest 或 commit 能力。

Space 授权严格绑定当前认证主体：优先使用 `api.tenants.<tenant>.wiki_space`，否则使用显式共享的 `wiki.default_space`；仅默认租户在两者都未配置时兼容 `local`。请求 Space 必须精确匹配授权结果，管理员身份不会绕过该隔离。Slug 只允许规范的相对页面路径，可省略 `.md`；路径穿越、二次编码、反斜杠逃逸、隐藏段和非 Markdown 扩展在调用 reader 前被拒绝。

成功响应为结构化 JSON，包含 canonical `wiki://` URI、Space、Slug、可选标题和原始 Markdown，并设置 `Cache-Control: private, no-store` 与 `X-Content-Type-Options: nosniff`。首版不进行 HTML 渲染，避免引入脚本、危险 HTML 和外链协议清洗面；未配置 reader 返回 503，不存在页面返回 404，其他后端读取失败返回无上游细节的 502。README 与 `Sample/agent-api.http` 已增加调用示例。

API 回归测试覆盖租户 Space 成功读取、共享 Space 越权、非 Markdown、二次编码路径穿越、隐藏路径、reader 未初始化和页面不存在；全仓 `go test ./...` 及 `go test -race ./internal/api ./cmd/server` 均通过。服务装配已将 readiness runtime 同时作为只读页面 reader 注入，代码需重启 8088 后完成真实三个 PBL 引用页面及越权请求验收。

服务重启后的 8088 线上验收已完成。`/ready` 显示本地 BM25 Wiki 健康、419 页、无 stale snapshot；目标概念、课程来源和 Vanessa Ruales 三个页面分别通过 `/api/wiki/pages/local/...` 返回 200，canonical URI 与原 `wiki://local/...` 引用一致，正文大小分别为 2214、2767 和 1174 字节。响应包含 `Content-Type: application/json`、`Cache-Control: private, no-store` 与 `X-Content-Type-Options: nosniff`。

负向验证结果为：无凭证 401、错误 Space 403、非 Markdown 扩展 400、双重编码路径穿越 400、不存在页面 404。测试命令只输出 URI、Space、Slug、正文字节数及状态码，没有将 Wiki 正文写入验证日志。至此只读页面 API 已完成代码、单元/竞态测试和真实服务安全闭环。

## 二十二、P2 Team-aware JIT 检索路由

定位到 `team=kb_qa` 仍可能调用 `wiki_search` 的原因不在 Team Prompt，而在 Multi-Agent Coordinator 会先于 Team Planner 执行确定性 JIT 检索。旧的进程级优先级在 Wiki 可用时统一选择 Wiki，因此绕过了 `kb_qa` 已声明的 RAG-first 约束；“学术顾问”又会命中事实证据判定，稳定触发该路径。

JIT 路由现先应用 Team 数据源策略：`kb_qa` 在 RAG 已配置时固定选择 `rag_search`，仅在 RAG 未配置且 Wiki 可用时兼容回退 `wiki_search`；`wiki`、`wiki_graph`、`wiki_suggest` 继续保持 Wiki-first，且不会在 Wiki 工具不可用时静默改走 RAG。显式要求查询任务记忆的请求仍优先 `memory_search`，其他 Team 保留原有兼容顺序。

同时为 `kb_qa.planner` 增加显式工具白名单，仅开放 RAG、任务记忆、工作区读取、HTTP/Web 搜索和图片分析工具，不向 LLM Planner 暴露 `wiki_search`、`wiki_graph` 或 `wiki_suggest`。主 `teams.yaml` 与中文注释镜像已同步。回归测试覆盖 RAG/Wiki 同时配置、RAG 缺失回退、Wiki Team 固定路由、Coordinator 在 Planner 前执行的实际路径，以及 `kb_qa` 工具边界；全仓 `go test ./...` 与 `go test -race ./internal/planner ./internal/multiagent` 均通过。代码和 Team 配置需重启服务后生效；重启后应以原始 `team=kb_qa` 请求进行一次线上 Trace 验收，确认第一步为 `rag_search` 且日志中的当前 Team 为 `kb_qa`。

## 二十三、P2 Multi-Agent Team 日志一致性

线上 `team=kb_qa` 任务的 Multi-Agent 内部执行与 Writer 均使用正确 Team，但 Orchestrator 最外层完成日志错误显示进程默认 `wiki_suggest`。根因是 `runMultiAgentNext` 在进入 Coordinator 前直接读取 `GetTeamsConfig().ActiveTeam` 作为日志和 span 标签，没有优先使用任务已固定的 `task.Team`；`steps=9` 只是全局 Trace 序号，与 Team 选择无关。

Orchestrator 现统一通过任务级解析函数设置开始日志、完成日志和 `multiagent.team` span：显式任务 Team 优先，只有旧任务未保存 Team 时才兼容回退进程默认值。任务实际执行仍由 Coordinator 的同一 Team 选择规则控制。Planner、Replanner、Writer 以及受控 Wiki 计划日志中重复显式附加的 `team` 字段已移除，由任务快照派生的 `teamLogger` 唯一注入，避免 JSON 出现两个同名键。

新增测试覆盖“默认 Team 为 `wiki_suggest`、任务 Team 为 `kb_qa`”以及空任务 Team 回退默认值。全仓 `go test ./...` 和 `go test -race ./internal/orchestrator ./internal/multiagent` 均通过。代码需重启服务后验证；同一任务从 Orchestrator 开始、Multi-Agent 各阶段到 Orchestrator 完成日志都应只出现一次 `team=kb_qa`。
