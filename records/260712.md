新增场景
  - multiagent_replanner
  - task_finalizer
  - context_compressor
  - answer_verifier
  - memory_summarizer
  - rag_query_rewriter
  - rag_reranker

  行为

  - Replanner 可使用独立的强推理模型。
  - Legacy/Eino 可通过 task_finalizer 二次生成最终答案。
  - Trace 达到 context_compression_trace_threshold 后可进行上下文压缩。
  - MultiAgent Writer 输出可通过独立 Verifier 校验；不支持的答案会降为 low confidence。
  - 长期记忆可使用 LLM 提炼可复用事实。
  - RAG 查询可重写，候选记忆可重新排序。
  - 所有新增能力仅在对应 Scene 显式配置后启用。
  - LLM 调用失败均回退到现有确定性路径，不阻断任务。
  - Scene 新增、删除和模型修改支持热加载。
  - Embedding 查询与文档仍共享同一 Profile，避免向量空间不一致。
  - 审批、路径检查、SSRF 防护和预算判断未引入 LLM已按推荐顺序完成全部场景扩展。

  新增场景

  - multiagent_replanner：失败重规划可使用更强模型。
  - task_finalizer：Legacy/Eino 可独立生成最终答案。
  - context_compressor：Trace 达到配置阈值后自动压缩。
  - answer_verifier：对 MultiAgent Writer 答案做证据校验。
  - memory_summarizer：生成更适合长期检索的事实摘要。
  - rag_query_rewriter：检索前重写查询。
  - rag_reranker：对候选记忆语义重排。

  行为约束

  - 新场景只有在 llm.scenes 显式配置后才启用。
  - 调用失败均回退至原有确定性路径，不阻断任务。
  - 配置增加或删除后可热加载生效。
  - Context Compressor 默认 Trace 阈值为 8。
  - Answer Verifier 不修改答案，只在证据不足时降低置信度并记录问题。
  - Embedding Profile 未拆分，避免查询与文档向量空间不兼容。
  - 审批、路径校验、SSRF 防护等安全策略没有引入 LLM。

  示例已经写入 config.yaml：

  llm:
    context_compression_trace_threshold: 8

    scenes:
      multiagent_replanner:
        model: "agent-planner-strong"

      task_finalizer:
        model: "agent-writer"

      context_compressor:
        model: "agent-fast"

      answer_verifier:
        model: "agent-verifier"

      memory_summarizer:
        model: "agent-fast"

      rag_query_rewriter:
        model: "agent-fast"

      rag_reranker:
        model: "agent-reranker"

# 2026-07-12
- 新增统一 internal/llm/structured.go：
      - 统一 Scene 配置解析。
      - 统一结构化 JSON 调用入口。
      - 自动记录 scene/provider/model/token usage OpenTelemetry 属性。
      - Memory 不再依赖 MultiAgent。

  - Finalizer 改为专用协议：
      - 不再复用 Planner Prompt、工具列表和决策 Schema。
      - 使用完整 Trace 和 Memory。
      - 只返回 final_answer/evidence_summary/confidence。
      - 输入限制为 64,000 个 Unicode 字符。

  - RAG Usage 与输入边界：
      - Query Rewrite、Reranker TokenUsage 写入 rag_prepare Trace。
      - 纳入 TokenBudget、指标和持久化汇总。
      - Reranker 候选字段和总输入均有限制。
      - 所有截断改为 Unicode Rune 安全截断。

  - Context Compressor 改为滚动摘要：
      - 使用 context_summary Trace 保存摘要。
      - 只压缩上次摘要之后新增的 Trace。
      - 新增 Trace 未达到阈值时复用旧摘要，不重复调用 LLM。
      - 增加“不重复压缩”回归测试。

  - 配置校验：
      - 拒绝未知 Provider。
      - LiteLLM 必须配置 BaseURL。
      - Model、Timeout、压缩阈值进行校验。
      - ADK 场景只允许 Gemini。
      - 热加载校验失败时保留旧配置。

  - 配置状态 API：
      - /api/config/reload 新增脱敏的 active_scenes。
      - 返回每个 Scene 的 Provider、Model、BaseURL 和 Timeout。

  - LiteLLM 契约测试：
      - 验证 /v1/chat/completions 请求格式。
      - 验证 structured output 和 TokenUsage 解析。
      - 使用无网络 RoundTripper，兼容受限 CI。

新增功能

  - 场景级回退：

  llm:
    scenes:
      task_planner:
        model: "agent-planner"
        fallback_scene: "task_planner_fallback"

      task_planner_fallback:
        model: "agent-planner-backup"

  - 配置校验会拒绝：
      - 不存在的 Fallback Scene。
      - 直接或间接循环回退。
      - 负数重试和预算策略。

  - 场景级重试：

  max_retries: 1

  失败调用和重试调用的 TokenUsage 会合并记录。

  - 可选场景预算门槛：

  min_remaining_tokens: 2000

  当前作用于：

  - Task Finalizer
  - Context Compressor
  - Answer Verifier
  - RAG Query Rewriter
  - RAG Reranker

  剩余 Token 不足时跳过增强调用，使用原有回退路径。

  - 新增 readiness：

  GET /ready

  返回：

  {
    "ready": true,
    "llm_scenes": {
      "task_planner": {
        "provider": "litellm",
        "healthy": true
      }
    }
  }

  LiteLLM 场景会将 Chat Completions BaseURL 转换为 /health 并执行最长 3 秒的探测。非 LiteLLM Provider 当前以配置校验结果作为健康基线。

  - 统一调用 Trace 新增：
      - llm.fallback.triggered
      - llm.fallback.scene
      - llm.retry.attempt
      - Scene、Provider、Model 和 TokenUsage

  测试

  新增覆盖：

  - Fallback 成功及 Usage 合并。
  - Fallback 循环和未知目标校验。
  - 重试次数及 Usage 合并。
  - min_remaining_tokens 跳过规则。
  - LiteLLM 健康 URL 转换。

## 2026-07-12 19:20

### 1. 内存存储（ MemoryStore ）异步索引去重与并发安全优化 (Bug #12)

  • 现状问题：在  MemoryStore.SaveFullTask  中，对于已完成的任务会异步启动 Embedding 计算并索引为 RAG
  长期记忆。由于异步任务的耗时性（如需请求远程接口），若外部快速连续调用  SaveFullTask ，会在第一个协程尚未写入  m.memories  前多次触发
  SaveFullTask ，从而并发启动重复的 Embedding 计算协程。
  • 改进方案：
      • 在  MemoryStore  结构体中新增  indexing map[string]bool  字典，用于同步标记与阻断正在进行中的异步 Embedding 计算。
      •  SaveFullTask  在写锁保护下原子性判断  !alreadyIndexed && !alreadyIndexing  并预先将  indexing[task.ID] = true  占位，任务执行完成后再在
      defer  中删除。
      • 为此新增单元测试 memory_test.go，覆盖高并发调用时的去重及安全性，运行并通过  -race  检查。


  ### 2. Redis 存储（ RedisStore ）分布式多实例迁移锁优化 (Bug #18)

  • 现状问题：当对旧版本索引数据进行  tasksIndexV2  平滑迁移时（ ensureTaskIndexes ），原本仅使用了进程内的  migrationMu sync.Mutex
  互斥锁。如果生产环境有多实例部署，各实例会同时检测迁移状态并执行数据重建，存在并发竞态与数据库写入放大。
  • 改进方案：
      • 使用 Redis 的  SetNX  原语设计了分布式锁机制（带 5 分钟超时防死锁），配合 Ticker
      轮询机制，确保任一实例在做索引迁移时，其他实例均挂起并静默等待迁移标志位，避免重复执行数据回填。


  ### 3. RAG 余弦相似度计算维度不匹配（CosineSimilarity）调试优化 (Bug #14)

  • 现状问题： CosineSimilarity  在遇到本地 128 维与远程高维模型向量（如 768 / 1536 维）混合查询时，会直接静默返回  0 ，使得 RAG
  回路处于退化状态且无任何排查线索。
  • 改进方案：
      • 对  CosineSimilarity  引入显式检查，当检测到两个非空向量的维度不匹配时输出  log.Warn  警告日志。
      • 重构了  MemoryStore  中的  cosineSimilarity  本地拷贝，将其统一收拢至并复用  internal/memory.CosineSimilarity ，减少冗余代码。