# ai-agent 会话续接摘要

更新时间：2026-09-15（America/Los_Angeles；保留原文件名供续接引用）

仓库：`/Users/xujunwu/Documents/IDEAProject/ai-agent`

分支：`main`

审查基线：`a43bdb3 feat: support selecting alternate config files`

历史修复提交：`3d001f0 fix: harden task execution and runtime boundaries`（2026-09-11，47 个文件）。B01–B13 修复、PostgreSQL 旧 trace 迁移补测和 SQL/O01 补强均已提交；O02 第一批已提交为 `92adfe8`，本轮 O03 的工作区状态见“最新进度”。

## 当前目标

本会话围绕项目审查报告中的 B01–B13、SQL 只读边界和 O01 依赖可达性扫描完成修复与验证。B01–B13 的 PostgreSQL/pgvector、本机回归和远端 Linux CI 已在 `420db0d` 验证。SQL/工具链与 O01 补强已提交为 `0301694`，其 [CI 34825817102](https://github.com/wuxujun/ai-agent/actions/runs/34825817102) 已通过。O02 第一批“审批恢复契约 + CI”已在 `92adfe8` 提交并推送，[CI 34918780196](https://github.com/wuxujun/ai-agent/actions/runs/34918780196) 的 3 个 job 全部通过。当前推进已获用户确认的 O03 第一批“外部 RAG 接收限制”；Windows 发布明确暂缓，缓存总预算和 O04 不在本批范围。

## 最新进度（2026-09-14）

- 新增 RAG 接收 helper：成功响应 4 MiB、错误正文读取 4 KiB；限制覆盖普通 HTTP、JSON-RPC、notification 和传输层自动解压。超限不解析、不返回部分结果。
- HTTP 错误只保留状态和有限前缀内的 MCP 初始化提示分类；保留原有自动回退，传播读取期间的取消错误。SSE 发现增加 64 KiB 单行/4 MiB 累计预算，预算违反不触发 POST 回退。
- SSE 发现超时或取消会关闭阻塞读取并等待退出；成功发现后解除 3 秒计时器，保留连接到后续 POST 完成。新测试覆盖实际 chunked/gzip、真实 HTTP 取消、超时关闭顺序和虚拟时间超过 3 秒的成功 POST 会话。
- 先失败后通过的回归及独立审查已完成；最终全量通过 48 个测试包（另 5 包无测试），memory/llm/tools/server 的 4 包 race、全仓 vet 和格式/diff 检查通过。首次全量因沙盒禁止 `httptest` 监听失败，获准在沙盒外复跑通过。
- 有限基准完成 3 次：8 MiB/64 MiB 超大输入均在相同预算处拒绝，接收 helper 累计分配约 10 MB/次；不含 JSON 解码，不代表峰值内存或生产性能。准确范围和命令已记录在审查报告。
- 本批交付流程已确认为只提交 O03 文件并推送到 `main`，随后核验该提交的 Linux CI；远端结果按本次提交 SHA 查询，不用上一批 CI 代替。工作区其他并行改动不纳入本批，提交前测试基于暂存区的独立快照执行。
- 详细预算、兼容范围和验证限制见[审查报告 O03 第一批](./项目审查报告_2026-09-08.md#o03-第一批外部-rag-接收字节预算2026-09-14)。接收上限不等于堆内存或解码对象预算，缓存和统一指标仍留后续批次。

## 历史进度：O02 第一批（2026-09-14）

- 新增 SQLite/PostgreSQL 共用的审批恢复 SQL 契约：关闭原连接后重建客户端，验证批准参数整体替换、`nil`/空 map 语义、竞争恢复、消费后的崩溃窗口、不丢 trace、回调发布后的可读性、HTTP task 回读，以及提交前失租不覆盖 peer 结果。共 5 个场景，不改运行时或 API。
- CI 从固定 10 项名单改为 `internal/...` 下的 `TestExternal*` 自动发现，加上既有审批总线用例；共选中 15 项，其中 ParadeDB 是缺少 `pg_search` 时明确跳过的可选测试。补回 PostgreSQL 审批 CAS、UTF-8 和 Redis Brain JSON 测试，新增恢复契约也被自动选中。
- Redis 旧用例改为 `TestExternalRedisTaskBrainFieldsJSONRoundTrip`，并修正首次实际纳入执行后发现的“先关客户端、后删测试夹具”顺序错误。修复后该真实 Redis 用例 `-race -count=5` 通过；新 SQLite 契约 `-race -count=10` 通过。
- 独立审查已结束：发现的 Redis 命名遗漏、崩溃窗口覆盖缺口和测试清理顺序均已核实处理，无剩余审查问题；生产文件的临时反向验证改动全部恢复。
- 最终串行全量退出 0（48 个测试包、5 个无测试包），API/orchestrator/store 的 3 包 race、vet 和格式/diff 检查通过。此前多套 Go 验证同时运行时有少量未改动包被启动驱动超时终止，失败日志保留，不计为通过；串行全量已正常通过这些包。
- 最终完整外部命令退出 0：14 项必跑契约通过，只有缺少 `pg_search` 的 ParadeDB 用例按预期跳过。本批交付流程已确认为提交并推送到 `main`，随后核验该提交的 Linux CI；远端结果应按提交 SHA 查询 GitHub Actions，不用本机结果代替。详细范围、命令和限制见[审查报告 O02 第一批](./项目审查报告_2026-09-08.md#o02-第一批审批恢复持久化契约与-ci2026-09-14)。
- 本轮专用 PostgreSQL/Redis 容器已停止，状态均为 `exited`；容器和卷保留，未操作其他业务服务。

## 历史进度（2026-09-13）

- 先前提交：`8e5cc07` SQL 初步只读修复、`1e18ab5` 文档、`bfa3618` grpc/text 依赖升级。不要把提交本身当作测试通过证明；此前 Go 1.25.0 全量测试最终超时退出 1。
- 本轮 Go 最低版本更新为 1.26.8；CI、外部集成、扫描和 release 均读取 `go.mod`，README 同步最低版本。系统全局 Go 设置未改动。
- SQL 正式回归发现并修复初步实现的漏项：编码文件名越界读取、直接 Execute 绕过校验、EXPLAIN PRAGMA、紧邻字面量/未闭合方括号，以及输出未计列名和分隔符。独立审查另发现 BOM 可隐藏 PRAGMA，已补先失败后通过的测试，拒绝字符串/注释外的 BOM。每条 SQLite 连接通过固定 URI 参数初始化只读；测试验证双连接和主数据库拒绝写入。
- `internal/tools/sql_query_security_test.go` 和 `sql_query_contract_test.go` 随修复一同提交，覆盖路径、方言、缺失文件、错误/取消、100 行/1 MiB 结果和 planner→executor 证据合约。多语句只读兼容保留；结果上限不代表 SQLite 驱动峰值内存上限。
- 本机 Go 1.26.8 使用 `GOARCH=arm64 CGO_ENABLED=1` 和 `-exec '/usr/bin/arch -arm64'` 的最终全量测试退出 0（48 包通过，5 包无测试），`go vet ./...`、diff/YAML 检查通过。同样启动方式的 race 退出 0，44 个测试包通过（`./cmd/llm-eval ./internal/... ./cmd/server`）。amd64 复跑仍有 3 包启动超时，保留失败日志；完整命令和最终日志见审查报告顶部。
- `govulncheck v1.8.0 -show verbose ./...`：darwin/amd64 可达漏洞 0 项；另有 1 项已导入包告警、5 项模块告警未发现受影响符号调用。具体 GO 编号和范围见审查报告顶部，不能称为“全部依赖无漏洞”。
- CI 增加独立扫描 job，固定扫描器版本并保留带版本/时间的报告 artifact。远端结果需查看本轮提交的 GitHub Actions；2026-09-12 的成功结果不覆盖此次变更。

## 已完成

### B01–B04：高优先级执行、审批和并发问题

- B01：审批恢复前解密并验证最终批准结果。批准结果中的非 nil 参数整体替换动作参数，空 map 也会持久化；缺失、损坏、拒绝或无效结果不会消费审批或执行任务。
- B02：Memory、SQLite、PostgreSQL、Redis 增加原子 `CreateTask` 能力。API 删除先查后写的竞态，重复自定义 ID 返回 409；不支持 `TaskCreationStore` 的自定义 Store 返回 501。
- B03：SSE `Publish` 与 `Unsubscribe` 在同一生命周期锁内完成，避免向已关闭 channel 发送；保留慢订阅者的非阻塞行为。
- B04：`run-all` 和 HTTP/启动扫描审批恢复统一使用可续租执行租约。续租失败、租约丢失或独立截止计时器到期会取消执行，并阻止旧 owner 写入任务或状态；保存和状态转换带 owner/期限校验。获取租约后重新读取任务，流式断连和关闭回滚也不会覆盖新 owner 的结果。

### B05–B13：记录、Redis、边界和配置问题

- B05：SQL trace 按完整 trace 快照的数组位置保存，新增可空 `execution_step` 保留公开的逻辑 `Step`，相同逻辑步数的多个审计事件不再互相覆盖或丢失。
- B06：Redis 状态 CAS 在同一 Lua 脚本中维护全局和租户状态索引；引入 v4 索引命名空间、带 owner 的迁移锁、分批扫描和从当前任务 JSON 修复索引。
- B07：请求体在 handler 执行前限制为 10 MiB，覆盖已知长度和 chunked 请求；HTTP server 加入 10 秒请求头、30 秒请求读取、60 秒空闲连接超时，未设置 SSE 写超时。
- B08：LLM 流式输出累计内容在缓冲和 callback 前限制为 4 MiB；命令 stdout/stderr 各保留最多 1 MiB，超出部分继续排空并在返回文本中标记截断。
- B09：SSE 终态缓存使用单个 TTL 计时器主动清理，最多 1024 条，每条文本最多 64 KiB；含审批或超大事件依赖实时事件和 Store 回读。
- B10：热重载拒绝启动绑定配置变化并保留原快照和 revision；空 MCP 列表的 nil/empty 表示等价。查询上限、Wiki space、缓存和熔断参数等动态设置仍可热更新。
- B11：OpenAI/Ollama embedding 错误响应只读取有限字节并返回状态错误，不把上游正文带入错误；成功响应限制为 4 MiB，解析错误不返回原始正文。
- B12：严格 JSON schema 正确处理 `additionalProperties`：false 拒绝未知字段，true 或省略允许，schema 对象会验证额外字段值。
- B13：自定义 recovery 只记录 panic 类型、路径、请求/trace ID 和栈，不格式化 panic 值，也不输出原始请求头或请求体；已写响应和 EPIPE/ECONNRESET 路径有专门处理。

## 历史验证（2026-09-12）

- 按用户指示从 `.env` 的 `AI_AGENT_STORE_DSN` 读取连接信息，连接已有 `127.0.0.1:5432` 服务。未输出密码或完整 DSN，未调用真实 LLM。
- 环境为现有 `paradedb` 容器中的 PostgreSQL 17.7、pgvector 0.8.0，以及临时 Redis 7.2.6。它们与 CI 的 pgvector 0.8.2、Redis 7.4.2 有版本差异，本机结果不冒充同镜像 Linux CI 结果。
- 在独立 `audit_validation_20260912_1hyr0b` schema 中执行数据库合约；未创建新数据库或角色。Redis 仅监听 `127.0.0.1:16397`、无持久化。完成后测试 schema（7 张测试表）已清理，Redis 已停止；再次连接确认原 PostgreSQL/pgvector 正常。
- CI 选择的 10 项外部测试全部通过 `-race -v -count=1 -timeout=3m`，无跳过：创建判重、失租写入保护、会话隔离、暂停任务持久化、trace 序列、旧 schema 升级、pgvector 排序、Redis 状态索引/旧索引迁移和跨客户端审批总线。
- `TestExternalPostgresMigrationUpgradesLegacySchema` 新增真实旧 trace 数据：迁移后 `execution_step` 为 NULL 时保留逻辑步数 `0、7`；追加同为第 `7` 步的审计事件，重复保存后物理序号为 `1、2、3`，逻辑步数为 `0、7、7`，`StepCount=7`、`MaxSteps=10` 不变。增强后的外部合约已复跑通过。
- 使用 `GOARCH=arm64 CGO_ENABLED=1 GOCACHE=/private/tmp/ai-agent-audit-go-cache`，最终 `go test ./... -count=1 -timeout=3m` 通过：48 个测试包、5 个无测试包；`go vet ./...` 通过。
- 本机 9 包 race 最终全部通过。直接启动方式曾被测试驱动在 4 分钟后终止，未输出 Go 测试栈；确认测试二进制为 ARM64 后，仅增加 `-exec '/usr/bin/arch -arm64'`，同一批 race 全部正常完成。本机 Go 驱动为 amd64；此前另一次 amd64 测试曾采样到 Rosetta 启动等待，不能把这些启动失败误报为业务测试失败或通过。
- 修复和测试补充已推送到 `origin/main`；提交 `420db0d976eaf7c8041fcb07cb745cd1e56172e9` 的 [GitHub CI 34727726907](https://github.com/wuxujun/ai-agent/actions/runs/34727726907) 已成功。[test job](https://github.com/wuxujun/ai-agent/actions/runs/34727726907/job/103644761275) 的格式、vet、全量、race、Compose 和 Secret scan 均成功；[外部集成 job](https://github.com/wuxujun/ai-agent/actions/runs/34727726907/job/103644761353) 的容器初始化、External backend contracts 和容器停止均成功，补齐配置的 pgvector 0.8.2/Redis 7.4.2 在 Linux 上的验证。

验证命令（外部测试的 `TEST_POSTGRES_DSN` 在进程内设置为隔离 schema 的 URL；不得直接把业务 schema 作为测试目标）：

```bash
GOARCH=arm64 CGO_ENABLED=1 GOCACHE=/private/tmp/ai-agent-audit-go-cache \
AI_AGENT_RUN_EXTERNAL_INTEGRATION=false TEST_POSTGRES_DSN='' TEST_REDIS_URL='' \
go test ./... -count=1 -timeout=3m

GOARCH=arm64 CGO_ENABLED=1 GOCACHE=/private/tmp/ai-agent-audit-go-cache \
AI_AGENT_RUN_EXTERNAL_INTEGRATION=false TEST_POSTGRES_DSN='' TEST_REDIS_URL='' \
go test -exec '/usr/bin/arch -arm64' -race \
  ./internal/api ./internal/config ./internal/llm ./internal/memory ./internal/tools \
  ./internal/store ./internal/orchestrator ./internal/multiagent ./cmd/server \
  -count=1 -timeout=3m

GOARCH=arm64 CGO_ENABLED=1 GOCACHE=/private/tmp/ai-agent-audit-go-cache \
AI_AGENT_RUN_EXTERNAL_INTEGRATION=true \
go test -race -v -count=1 -timeout=3m ./internal/store ./internal/orchestrator \
  -run '^(TestExternalPostgresTraceSequence|TestExternalRedisStatusTransitionMaintainsTenantIndexes|TestExternalRedisRebuildsLegacyStatusIndexes|TestExternalStoresTaskCreation|TestExternalStoresTaskLeaseGuard|TestExternalStoresSessionLeaseAndIsolation|TestExternalPostgresPGVectorRanking|TestExternalPostgresMigrationUpgradesLegacySchema|TestExternalStoresPersistPausedTaskAcrossClients|TestApprovalBusAcrossRedisClients)$'
```

本轮脱敏日志位于 `/private/tmp/ai-agent-pg-validation-20260912-1hyR0B/`：`external-contracts.log`、`full-test-final.log`、`race-native-exec.log`（最终通过）和 `race.log`（首次超时，保留诊断）。临时日志可能被系统清理；正式回归断言已保存在仓库测试中。

## 历史验证（2026-09-11）

使用 `GOCACHE=/private/tmp/ai-agent-audit-go-cache`：

```bash
go test ./...
go test -race ./internal/api ./internal/config ./internal/llm ./internal/memory ./internal/tools ./internal/store ./internal/orchestrator ./internal/multiagent ./cmd/server
go vet ./...
git diff --check
```

结果：

- 全量测试通过：48 个测试包，5 个无测试包。
- 受影响模块 race 测试通过：9 个包。
- `go vet ./...` 通过。
- `git diff --check` 通过。
- 所有当前变更和新增 Go 文件均通过 `gofmt -l` 检查。

外部 Redis 验证使用临时 Redis 7.2.6，仅绑定 `127.0.0.1:16397`、无持久化，验证了任务创建、租约保护、会话隔离、暂停任务持久化、状态索引 CAS 和旧索引迁移；测试后服务已关闭。未使用真实 LLM，也未读取真实密钥。

相关日志保存在临时目录：

- `/private/tmp/p2-full-test.log`
- `/private/tmp/p2-race.log`
- `/private/tmp/p2-vet.log`
- `/private/tmp/p2-redis-final.log`

## 未完成和验证边界

1. SQL 专项已补正式回归。结果字节预算不限制 SQLite 引擎/驱动的峰值内存；仍使用项目已有路径策略，未新增抵御宿主并发替换符号链接的文件句柄沙箱。
2. 没有进行生产环境双实例故障演练，也没有验证 Linux/systemd 部署行为；当前双 Handler 测试是进程内组件验证。
3. B04 失租后的取消依赖下游工具遵守 context；已经开始的外部副作用无法撤回。
4. B05 的 `traces.execution_step` 是可空新增列。升级时应备份数据库、停止旧版本写入，避免新旧二进制混写；旧版本回滚可能误解新 trace 序号，修复前已丢失的 trace 无法自动恢复。完整快照比较的长 trace 性能尚未专项基准测试。
5. Redis v4 索引首次使用时会惰性迁移；旧索引保留且程序不会批量删除。所有写入实例应先统一升级，旧版本状态 CAS 不会维护 v4 索引；大量存量数据的首次迁移延迟尚未专项压测。
6. 已运行 `govulncheck` 并加入 CI；生产性能基准或真实服务端到端测试未完成。
7. O01 已实现；O02–O04 尚未全面实施：O02 已补 SQL 工具和审批恢复持久化契约，O03 第一批只补 RAG 接收限制；更多跨层契约、解码对象/缓存总预算、统一指标和按职责拆分仍待后续处理。
8. 本轮补查发现 Windows/amd64 发布编译在既有 `internal/brain/retraction.go` 的 Unix 专用调用处失败，需单独做平台兼容处理；Linux/amd64 无 CGO 发布目标编译已通过。

## 变更归属

以下 B01–B13 变更已包含在 `3d001f0`。续接时以实时 `git status --short --branch` 为准，不再把历史文件数量视作当前未提交改动：

- `.github/workflows/ci.yml`：外部 PostgreSQL/Redis 合约测试选择范围。
- `Sample/agent-api.http`：原子创建、租约 run-all、10 MiB 请求限制、配置 reload 和 trace 语义示例。
- `config.yaml`：Wiki 启动绑定与动态配置说明注释。
- `internal/api`、`internal/orchestrator`、`internal/store`：四批次运行时修复和回归测试。
- `internal/config`、`internal/llm`、`internal/memory`、`internal/tools`、`cmd/server`：配置、响应、输出和 HTTP 限制修复。
- `docs/项目审查报告_2026-09-08.md`：完整审查报告及 B01–B13 修复记录。

2026-09-12 的补充仅涉及 `internal/store/external_integration_test.go` 和两份审查/续接文档，没有再次修改 B01–B13 运行时代码。

不要使用 `git reset --hard`、`git checkout --` 或批量删除命令。工作区中原有的用户文件也应保留。

## 建议的新会话起点

新会话开始后按以下顺序继续：

1. 读取本文件和 [项目审查报告](./项目审查报告_2026-09-08.md)。
2. 检查 `git status --short` 和 `git diff --check`，确认没有新的用户改动被覆盖。
3. 核查本轮提交及远端 CI，尤其新增 vulnerability-scan 和 Go 1.26.8 下的 Linux 外部合约。SQL 专项已保存在正式测试中，不再使用旧临时探针的“必须通过 planner 才算复现”断言作为修复后验收。
4. 部署前继续遵守 SQL 备份、停止旧版本写入、禁止混写及 Redis 写入实例统一升级的要求；本轮测试不证明旧二进制可直接回滚。
5. 后续按风险和收益安排 O02–O04；保留扫描中尚未可达的依赖告警，后续调用关系变化由 CI 持续检查。

## 参考文件

- [项目审查报告](/Users/xujunwu/Documents/IDEAProject/ai-agent/docs/项目审查报告_2026-09-08.md)
- [API 示例](/Users/xujunwu/Documents/IDEAProject/ai-agent/Sample/agent-api.http)
- [CI 配置](/Users/xujunwu/Documents/IDEAProject/ai-agent/.github/workflows/ci.yml)
